<article class ="card">
    <div class = "card-header">
    <a href = "/post/{{id}}">
    <p>Posted By: {{user.username}} on {{format_date created_at}}</p>
    </a>
    </div>
    <header class ="card-body">{{title}}</header>
    <p class ="card-body">{{content}}</p>
</article>